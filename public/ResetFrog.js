function ResetFrog() {
  if (nLives == 0) {
    //end game
  } else {
    frog.left=width/2;
    frog.bottom=height-grid; 
    //tlength=timeAllowed;
    //timePlayed=false;
    //timer=0;
    //c1=green;
    //fanfare.play();
    //if (!top  && lives>0) {lives=lives-1;}
  }
}